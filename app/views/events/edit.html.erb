<%= form_for @event do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= f.submit "Update" %>
<% end %>
<%= link_to 'Back to index', :controller => 'events', :action => 'index'%>
